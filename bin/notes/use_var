Як в dynamic, так і var, змінна може містити дані будь-якого типу даних, тобто int , float, string тощо.
Var - це лише ключове слово, яке використовується для оголошення змінної.

var x = "anc"; - компілятор зрозуміє, що ви бажаєте надати змінній х тип String

Якщо оголосити змінну var один раз, призначений тип не може змінюватися:
void main() {
  var x = 'abc';
  x = 12345; - помилка про те, що значення типу 'int' не може бути присвоєно змінній типу String
  print(x);
}

Але, якщо вказати var без ініціалізації, він стає динамічним:
 void main() {
   var x ; - присвоєно динамічний тип
   x = 'abc';
   x=12345; - помилки не виникне
   print(x);
 }

Якщо змінна оголошена динамічною і навіть ініціалізована, її тип може змінюватися з часом.

 void main() {
  dynamic x = 'abc';
  x = 12345;
  print(x);
}

Динамічний тип вимикає статичну перевірку типів для змінної. Засоби компілятора не будуть хвилювати
тип динамічної змінної:

dynamic x = 12;
x.length() - Exception thrown at runtime

Зазначений вище код скомпілюється успішно, але в процесі виконання виникне помилка:
since length() is a function of String type. Топу тип dynamic потрібно використовувати з обережністю.
